<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <h1>Home</h1>
    <% for (let i = 0; i < product.length; i++) { %>
    <p>
        Name:
        <a href = '/product/<%= product[i].id %>'>
            <%= product[i].name %>
        </a>
    </p>
    <p>
        Price:
        <%= product[i].price %>
    </p>
    <img class = 'inImage' src = "<%= product[i].image %>"/><br>
    <form action = '/product/<%= product[i].id %>?_method=DELETE' method='POST'>
        <input type = 'submit' value = "Delete"/> 
    </form>
    <% } %>
    <a href = "/product/new">Create New Product</a>
</body>
</html>

